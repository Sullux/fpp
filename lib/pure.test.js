const { Pure, isPure } = require('./pure')

describe('pure', () => {
  describe('Pure', () => {
    it('should return the original function', () => {
      //
    })
    it('should return a constant function', () => {
      //
    })
  })
})
